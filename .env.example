# ---------------------------------------------------------------------------
# Image versions - pin these so labs behave the same for every student run
# ---------------------------------------------------------------------------
HADOOP_IMAGE_TAG=2.0.0-hadoop3.2.1-java8
SPARK_IMAGE_TAG=3.3.0-hadoop3.3
KAFKA_IMAGE_TAG=3.9.2
KAFKA_UI_IMAGE_TAG=latest
AIRFLOW_BASE_IMAGE=apache/airflow:2.9.3-python3.11
POSTGRES_IMAGE_TAG=13

# Spark client library version installed in Jupyter/Airflow - MUST match SPARK_IMAGE_TAG's
# Spark version (3.3.0 here) or SparkSession() calls to the cluster will fail/hang.
PYSPARK_VERSION=3.3.0
# Matching Kafka connector package used in spark-submit --packages for streaming jobs
SPARK_KAFKA_PACKAGE=org.apache.spark:spark-sql-kafka-0-10_2.12:3.3.0

# ---------------------------------------------------------------------------
# Ports (host side) - change here if something collides on your laptop
# ---------------------------------------------------------------------------
NAMENODE_UI_PORT=9870
DATANODE_UI_PORT=9864
HDFS_RPC_PORT=9000

SPARK_MASTER_UI_PORT=8080
SPARK_MASTER_PORT=7077
SPARK_WORKER_UI_PORT=8081

JUPYTER_PORT=8888

KAFKA_INTERNAL_PORT=9092
KAFKA_EXTERNAL_PORT=9094
KAFKA_UI_PORT=8090

AIRFLOW_WEBSERVER_PORT=8082

# ---------------------------------------------------------------------------
# Airflow admin user (change the password before real use)
# ---------------------------------------------------------------------------
AIRFLOW_ADMIN_USER=admin
AIRFLOW_ADMIN_PASSWORD=changeme
AIRFLOW_ADMIN_EMAIL=admin@example.com

# ---------------------------------------------------------------------------
# PostGIS (serving storage for the maritime platform)
# ---------------------------------------------------------------------------
POSTGIS_IMAGE_TAG=15-3.3
POSTGIS_EXTERNAL_PORT=5433
POSTGIS_DB=maritime
POSTGIS_USER=maritime
POSTGIS_PASSWORD=changeme

# ---------------------------------------------------------------------------
# Superset (visualization)
# ---------------------------------------------------------------------------
SUPERSET_IMAGE_TAG=3.1.3
SUPERSET_PORT=8089
SUPERSET_SECRET_KEY=change-me-please-superset-secret
SUPERSET_ADMIN_USER=admin
SUPERSET_ADMIN_PASSWORD=changeme
SUPERSET_ADMIN_EMAIL=admin@example.com

# ---------------------------------------------------------------------------
# Airflow webserver secret key (used to sign sessions and CSRF tokens)
# Change this before any non-local deployment.
# ---------------------------------------------------------------------------
AIRFLOW_SECRET_KEY=change-me-to-a-secure-random-key

# ---------------------------------------------------------------------------
# AIS ingestion (aisstream.io)
# Get a free API key at https://aisstream.io
# Bounding boxes format: "lat1,lon1,lat2,lon2;lat1,lon1,lat2,lon2"
# ---------------------------------------------------------------------------
AISSTREAM_API_KEY=
AIS_BOUNDING_BOXES=29.5,31.0,32.5,35.0

# ---------------------------------------------------------------------------
# On Linux, set this to your host user id so files written by the Airflow
# containers into ./airflow/logs and ./airflow/plugins aren't owned by root.
# Run:  echo "AIRFLOW_UID=$(id -u)" >> .env
# On macOS/Windows (Docker Desktop) you can leave this unset - it defaults to 50000.
# ---------------------------------------------------------------------------
AIRFLOW_UID=50000
